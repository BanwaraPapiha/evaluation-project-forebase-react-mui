import { Typography } from '@mui/material';

const Footer = () => {
    return (
        <Typography variant="subtitle2" align="center">
        @EVALUATION SYSTEM
        </Typography>
    );
}

export default Footer;
