import { Typography } from '@mui/material';

const Footer = () => {
    return (
        <Typography variant="subtitle2" align="center" style={{position: 'absolute', bottom: '5px', left: '40%'}}>
        @Evaluation System
        </Typography>
    );
}

export default Footer;
